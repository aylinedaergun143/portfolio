# Aylin Eda Ergün — Phase 3

Scope: the approved responsive homepage plus a responsive About page with Experience accordions, Education, Expertise, and Interests. Individual case-study routes are intentionally not implemented yet.

The brief is preserved in `docs/PROJECT.md`. The homepage is in `dist/index.html`, the About page is in `dist/about/index.html`, and shared styles are in `dist/styles/`.

This dependency-free static build retains the approved Phase 1 visual foundation and Phase 2 homepage. The typeface uses the local Helvetica Neue / Helvetica / Arial stack; no remote font dependency is required. Project images, contact details, and detailed experience descriptions were not supplied and remain explicitly marked placeholders.

Local preview: `ruby -run -e httpd dist -p 3000 -b 127.0.0.1`.

No deployment is included during this phased local review workflow.
